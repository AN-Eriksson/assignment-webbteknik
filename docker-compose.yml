services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3147:8080"
    environment:
      GITHUB_CLIENT_ID: ${GITHUB_CLIENT_ID}
      GITHUB_CLIENT_SECRET: ${GITHUB_CLIENT_SECRET}
      UFO_API_URL: ${UFO_API_URL}
